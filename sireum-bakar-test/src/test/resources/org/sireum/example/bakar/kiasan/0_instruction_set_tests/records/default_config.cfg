package Standard is
  -- This config is currently being used by 
  -- org.sireum.alir.profile.spark.test.SPARKConstAttributeValueResolverTest
  -- in order to inject a default configuration for a test case when one is 
  -- not already present
   
  -- matching off of Java's int and long defs
  
  type Integer is range -2**15 .. 2**15-1;
  type Long_Integer is range -2**31 .. 2**31-1;
  
  -- TODO: add the other primitive numeric types
end Standard;