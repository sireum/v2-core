This alternative Integer_Set does not make use of the Local_Z_Set and helper
proof function M(). Instead it directly declares Z_Set as a local abstract
type and repeats the signatures of functions over Z_Set. This repetition is
unlikely to be something desirable.
