enum State {
  FRESH, STALE
}

enum State {
  FOO
}

procedure foo {
  # return (`State.FRESH`, `State.STALE`);
}

procedure bar {
  # return (`State.FRESH`, `State.STALE`, `Mode.SLOW`, `Mode.FAST`);
}

enum Mode {
  SLOW, FAST
}